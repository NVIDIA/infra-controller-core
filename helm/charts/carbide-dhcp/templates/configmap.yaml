{{- if .Values.config.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: carbide-dhcp-config
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "carbide-dhcp.labels" . | nindent 4 }}
data:
  kea_config.json: |
    {{- /* Replace __NAMESPACE__ placeholder with actual global.namespace */ -}}
    {{- .Values.config.keaConfigJson | replace "__NAMESPACE__" .Values.global.namespace | nindent 4 }}
{{- end }}
