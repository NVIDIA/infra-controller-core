apiVersion: v1
kind: Service
metadata:
  name: carbide-pxe
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "carbide-pxe.labels" . | nindent 4 }}
spec:
  selector:
    {{- include "carbide-pxe.selectorLabels" . | nindent 4 }}
  ports:
    - port: {{ .Values.service.port }}
      name: http
      protocol: TCP
